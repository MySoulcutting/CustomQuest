# =====================================================
# CustomQuest 全局配置
# =====================================================

# 玩家数据自动保存间隔（秒）
autosave-seconds: 300

# -----------------------------------------------------
# 任务追踪（SoulCore HUD / 原生计分板回退）
# HUD 快照固定每 5 秒重发一次，供客户端检测失效后端并清理残留（当前不可配置）
# -----------------------------------------------------
scoreboard:
  # 任务追踪总开关：SoulCore 客户端显示 HUD，其余客户端回退到计分板
  # false 会同时清空 HUD 与本插件设置的计分板
  enabled: true
  # 回退计分板标题（支持 & 颜色代码）
  title: "&6&l任务追踪"
  # 回退计分板中每个任务之间的空行数（0 = 无空格）
  gap-lines: 1
